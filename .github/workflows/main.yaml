name: CI

on: push

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: empaquetar
        run: mvn package -DskipTests=true

  deploy:
    needs: qa
    runs-on: self-hosted
    steps:
      - name: despliegue
        run: echo "despliegue"
